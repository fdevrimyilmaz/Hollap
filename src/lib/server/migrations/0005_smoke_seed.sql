-- Demo seeding has been retired. Keep this migration version as an idempotent
-- no-op so existing environments remain compatible while fresh installs start
-- with an empty, production-like dataset.
SELECT 1;
